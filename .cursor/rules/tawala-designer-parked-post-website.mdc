---
description: Parked Designer polish until the website is operational — do not start these items early
alwaysApply: true
---

# Designer parked post-website pass

**Do not start** the items below until the website is operational. Site work affects some Designer functions. Then run another Designer pass.

## MUST DO when Designer resumes (owner Jul 31, 2026)

**Conflate Insert → Invitation… and Insert → Hyperlink…** into one Insert Link dialog — **pride of place to Form-in-project links** (today’s Invitation). External URL (Hyperlink) = secondary mode; private InviteeID = tertiary. Deploy XML stays `<invitation>` vs `<link>` by mode. **DONE Aug 10** — Insert → **Link…**; dual menu peers removed.

- Spec: `Tawala_Key_Documents/DESIGNER_INSERT_MENU_AND_FUNCTIONS.md` § **Insert → Link / Invitation… (unified — Form links first)**
- Also noted in: `docs/CHAT_HANDOFF.md` Chat 1 opener; triage note on *Private Invitations.doc*

**Rename Designer Deploy → Push (owner Aug 10, 2026 — DONE Aug 10):** User-facing **Deploy…** / **Deploy this version** → **Push…** / **Push to My Tawala** / **Push this version**. Keep `/api/deploy`, `deployProject`, and related code ids. Inventory: `website-mock/README.md` § **Designer Push rename checklist**. Website My Tawala Details keeps the verb **Deploy** (go live + share/embed).

**Theme applies on Push (owner Aug 10, 2026 — DONE Aug 10):** My Tawala Theme dropdown persists `themePath` and, when a live definition exists, **Pushes** stamped project/form `themePath` to `:8080` via `/api/deploy`. **Push this version** stamps overlay Theme onto the definition before upload. Designer Push receipt carries `themePath`. Cross-ref: `website-mock/README.md` Task List #5.

## Parked until site is up

1. **Document smoothness P0s** — Face/Size multi-chip, invent-with-tables, chip drag, table reflow. **After live, before public (owner Aug 27).**
2. **Native `confirm()`** — still browser-native in places; **before public use** — nicer dialogs + chrome conform (owner Aug 27). See also gated **F2**.
3. **Font Color picker** — left intentional as-is; **Version 2 or never** (owner Aug 27).
4. **Skip/Process stubs** — Cut/Copy/Paste/Undo/Redo; Process If/Set still lack Cancel (Send has Cancel). **ASAP (owner Aug 27).**

## Also deferred (site-dependent)

- **MainMenu → Project:** **Project Manager** is live (My Tawala / website — owner Aug 27). **Email Delivery…** — hide menu stub; delivery path stays as-is. **Library-side** email record-keeping / billing counters per registered user (My Tawala group email volume); Test Drive sends negligible. See Task List **#20** HOLD in `website-mock/README.md`; `docs/EMAIL_DELIVERY_OPS.md`.
- **Sample JSON replacement** (Jul 28 conversions) — **ASAP (owner Aug 27):** re-check New Project samples; owner believes unhappy ones already replaced.
- **Page Header / banner graphics** — **Later (owner Aug 27):** full source image kept regardless of banner crop; re-edit always possible. Optional: min/max import size for quality standards.

## Owner-reported Jul 30 / Aug 11 — park (Not blocking for Live Library)

**Do not fix until return ~Aug 20 Designer pass** (owner traveling). Detail + screenshots: `Tawala_Key_Documents/DESIGNER_OPEN_BUGS.md` § “Parked Jul 30 / reconfirmed Aug 11”. Catchup: `docs/CATCHUP_MEMO_RETURN_AUG20.md`.

Owner policy: will not ship projects to **Library Live** that cannot be fixed until **blocking** Designer bugs are removed. These are explicitly **Not blocking** for Live Library (polish / authoring UX).

1. **FIB Styles — “Align right side” almost gone** — Project → Styles → FIBs → **Blanks**. Squashed control. Shots: `Bug_-_FIB-Styles-AlignRight-squashed-radio.png` + Aug 11 reconfirm.
2. **Form Text — paragraph blank-line separation lost on Deploy + image breaks highlighting** — Jul 30 stash (not in Aug 11 recreations). Shots: `Bug_-_Text-paragraph-spacing-*.png`.
3. **Form canvas badges — uneven widths** — Qn / `{Qn}` chips should share a straight right edge; **SKIP** may stick out with a distinct color. Shot: `Bug_-_Form-badge-uneven-widths-Aug11.png`.
4. **Sign-up Template — Right justified FIB labels bounce vertically** (mid vs bottom of blanks). Shot: `Bug_-_Signup-FIB-right-justified-vertical-bounce-Aug11.png`.
5. **Online Exam Builder SetupVariables — TinyMCE on multi-line FIBs** font size chaotic / cross-talk (live `:8080`). Shot: `Bug_-_OnlineExam-Setup-expanded-Text-font-size-chaos-Aug11.png`. **Fixed Aug 20** — see `DESIGNER_OPEN_BUGS.md`.

## Next focus (separate chat)

**Website — library mock & test-drive links** (`~/Projects/Tawala`) — Designer Sign-up / Get Together gate met; next is a fully operational site (library fidelity + stable `:8080` test-drive links).

Handoff: `docs/CHAT_HANDOFF.md` → Chat 3; also `docs/MORNING_START.md` §5, `docs/ROADMAP.md` Phase 3, `website-mock/README.md`.
