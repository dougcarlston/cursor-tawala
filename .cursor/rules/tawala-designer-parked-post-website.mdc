---
description: Parked Designer polish until the website is operational — do not start these items early
alwaysApply: true
---

# Designer parked post-website pass

**Do not start** the items below until the website is operational. Site work affects some Designer functions. Then run another Designer pass.

## MUST DO when Designer resumes (owner Jul 31, 2026)

**Conflate Insert → Invitation… and Insert → Hyperlink…** into one Insert Link dialog — **pride of place to Form-in-project links** (today’s Invitation). External URL (Hyperlink) = secondary mode; private InviteeID = tertiary. Deploy XML stays `<invitation>` vs `<link>` by mode. Dual menu peers are transitional only.

- Spec (do not re-debate framing): `Tawala_Key_Documents/DESIGNER_INSERT_MENU_AND_FUNCTIONS.md` § **Insert → Link / Invitation… (unified — Form links first)**
- Also noted in: `docs/CHAT_HANDOFF.md` Chat 1 opener; triage note on *Private Invitations.doc*

## Parked until site is up

1. **Document smoothness P0s** — Face/Size multi-chip, invent-with-tables, chip drag, table reflow
2. **Native `confirm()`** — still browser-native in places; polish later
3. **Font Color picker** — left intentional as-is; do **not** “fix” without an owner ask
4. **Skip/Process stubs** — Cut/Copy/Paste/Undo/Redo; Process If/Set still lack Cancel (Send has Cancel)

## Also deferred (site-dependent)

- **MainMenu → Project stubs:** Project Manager and Email Delivery — wait for site; not done
- **Sample JSON replacement** (Jul 28 conversions) — owner reviewing offline; not blocking
- **Page Header / banner graphics** — parked; Deploy has sophisticated image processing — do not interfere until understood

## Owner-reported Jul 30 — park (Not blocking for Live Library)

**Do not fix in website chat.** Post-website Designer pass. Detail + screenshots: `Tawala_Key_Documents/DESIGNER_OPEN_BUGS.md` § “Parked Jul 30 (Not blocking)”.

Owner policy: will not ship projects to **Library Live** that cannot be fixed until **blocking** Designer bugs are removed. These two are explicitly **Not blocking** for Live Library.

1. **FIB Styles — “Align right side” radio squashed** — Main Menu → Project → Styles → FIBs → **Blanks**. Labels radios (Above / Left / Right justified / Freeform) look normal; **Align right side** renders as a narrow vertical blue pill instead of a round radio. Shot: `Tawala_Key_Documents/assets/Bug_-_FIB-Styles-AlignRight-squashed-radio.png`.

2. **Form Text — paragraph blank-line separation lost on Deploy + image breaks highlighting** — Design shows blank lines between paragraphs (e.g. Multiple Question Survey instructional text); Deploy/runtime packs paragraphs with no blank-line gap. Also: inserting an image breaks selection highlight so it will not include paragraphs that contain the image or text beyond it. Shots: `…/Bug_-_Text-paragraph-spacing-Design.png`, `…/Bug_-_Text-paragraph-spacing-Deploy.png`.

## Next focus (separate chat)

**Website — library mock & test-drive links** (`~/Projects/Tawala`) — Designer Sign-up / Get Together gate met; next is a fully operational site (library fidelity + stable `:8080` test-drive links).

Handoff: `docs/CHAT_HANDOFF.md` → Chat 3; also `docs/MORNING_START.md` §5, `docs/ROADMAP.md` Phase 3, `website-mock/README.md`.
